import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Junaid Aurungzaib | Machine Learning & Computer Vision Portfolio",
  description:
    "Professional portfolio of Junaid Aurungzaib - machine learning, computer vision, full-stack software, and QA background."
};

export default function RootLayout({
  children
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
